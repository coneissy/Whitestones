services:
  - type: worker
    name: whitestones-bot
    runtime: python
    buildCommand: pip install -e .
    startCommand: python -m app.main
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
